package com.lec.ex2_rect;
// 클레스 : 속성(인스턴스변수 private), 생성자함수(속성데이터초기화), 메소드 (mathod), setter&getter
// Rect r1 = new Rect(); r1.setWidth(2); r2.setHeight(4);
// Rect r1 = new Rect(2, 4)
public class Rect {
	private int width;
	private int height;
	//생성자 함수가 없을 경우 , JVM이 자동생성
	public Rect(int w, int h) {
		
	}
	
}
