package com.lec.ex1_car;
// 클래스 : 속성(데이터=인스턴스변수)과 메소드
// Car myCar = new Car();
// myCar.method명() 호출
public class Car {
	String color;
	int    cc; //배기량 
	int    speed;
	public void park() {
		speed = 0;
		System.out.println(color + "색 주차함 . 지금속도 :" + speed);
	}
}
